name: Basic CI Test

on: 
 push:
   branches: 
     -main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: print sucess message 
        run: echo "everything is working perfectely"
